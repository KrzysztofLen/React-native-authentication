import React from 'react';
import {View, Text} from 'react-native';

const ProfileScreen = () => {
    return (
        <View>
            <Text>Welcome in the ProfileScreen!</Text>
        </View>
    );
};

export default ProfileScreen;
